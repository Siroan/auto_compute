[package]
name = "auto_compute"
version = "0.1.0"
authors = ["Antoine Menciere <antoine.menciere@gmail.com>"]

[dependencies]
compute = { path = "compute" }
compute-macro = { path = "compute-macro" }
